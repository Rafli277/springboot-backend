name: Build and Deploy Backend

on:
  workflow_dispatch:
  push:
    branches: [main] #kalo mau otomatis buka aja

# on:
#   push:
#     branches: [main]
#     paths:
#       - 'src/**'
#       - 'pom.xml'
#       - 'Dockerfile'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build -t your-username/springboot-backend:latest .

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push Docker image (backend)
        uses: docker/build-push-action@v6
        with:
          file: ./Dockerfile
          push: true
          tags: |
            rafli27/springboot-backend:latest

      # - name: Push to Docker Hub
      #   run: |
      #     echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
      #     docker push your-username/springboot-backend:latest

      # - name: Deploy to VM
      #   uses: appleboy/ssh-action@v0.1.6
      #   with:
      #     host: ${{ secrets.VM_HOST }}
      #     username: ${{ secrets.VM_USERNAME }}
      #     key: ${{ secrets.VM_SSH_KEY }}
      #     script: |
      #       cd /opt/app-infra
      #       docker pull your-username/springboot-backend:latest
      #       docker-compose up -d --no-deps backend
      #       echo "✅ Backend deployed successfully!"
